services:
  - type: web
    name: social-data-analyzer
    runtime: node
    plan: free
    buildCommand: "echo 'No build required'"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
